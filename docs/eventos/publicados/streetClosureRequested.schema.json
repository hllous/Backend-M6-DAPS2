{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://uade.m6.grupo04/eventos/streetClosureRequested.schema.json",
  "title": "streetClosureRequested",
  "description": "M6 → M7. Un servicio o una intervención requiere cortar la calle. Esquema unificado con la solicitud que M7 recibe de M3 (30/08).",
  "type": "object",
  "properties": {
    "closureRequestId": {
      "type": "string",
      "description": "Nuestro. Antes 'requestId'. Es el que le pedimos a M7 que devuelva como closureRequestId."
    },
    "sourceModule": {
      "type": "string",
      "const": "M6",
      "description": "M3 | M6 en el esquema unificado. Para nosotros, siempre M6."
    },
    "sourceRef": {
      "type": "string",
      "description": "El serviceId o interventionId que origina el corte."
    },
    "reason": { "type": "string" },
    "affectedSections": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Antes 'streets'."
    },
    "requestedFrom": { "$ref": "_shared.schema.json#/$defs/timestamp", "description": "Antes 'from'." },
    "requestedTo": { "$ref": "_shared.schema.json#/$defs/timestamp", "description": "Antes 'to'." },
    "closureType": { "type": "string", "enum": ["TOTAL", "PARTIAL"], "description": "Opcional en el esquema unificado; lo mandamos siempre." },
    "requestedAt": { "$ref": "_shared.schema.json#/$defs/timestamp" }
  },
  "required": ["closureRequestId", "sourceModule", "sourceRef", "reason", "affectedSections", "requestedFrom", "requestedTo", "requestedAt"],
  "additionalProperties": false
}
